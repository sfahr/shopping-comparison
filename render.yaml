services:
  - type: web
    name: shopping-comparison
    runtime: node
    buildCommand: cd shopping-comparison && npm install && npm run build
    startCommand: cd shopping-comparison && npm start
    envVars:
      - key: NODE_ENV
        value: production
